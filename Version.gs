/*** ================= LA VERSIÓN =================
 *
 * Un solo sitio para el número de versión. Hasta la BD v63 estaba escrito
 * dos veces, en Codigo.gs (VERSION, que sale en el pie de los PDF y en los
 * carteles) y en Formato.gs (VERSION_BD, que sale en el panel), y cada
 * cambio de versión obligaba a subir esos dos ficheros enteros, que son los
 * más grandes del programa. Desde la BD v64 se cambia aquí y en ningún
 * sitio más. El manifiesto carga este fichero el primero.
 * ======================================================== ***/
const VERSION = 'BD v64';
const VERSION_BD = VERSION;
